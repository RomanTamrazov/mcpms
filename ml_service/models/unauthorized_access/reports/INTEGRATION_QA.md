# Integration QA — unauthorized_access

Дата: 2026-09-19.

- Исходный `MANIFEST.sha256`: все файлы архива подтверждены.
- Окружение исходного пакета: CPython 3.11, версии из `requirements.lock`.
- Исходные тесты: 26 passed.
- Исходный и интеграционный Python-код: `ruff` passed.
- Полная пересборка: 57 464 939 строк журналов охранной подсистемы после
  фильтрации по каналам; объектная витрина 71 465 × 589, контекстная
  68 009 × 50, зональная 2 496 378 × 205.
- Восстановленный исходный режим `high_precision_75` точно совпал с отчётом:
  Precision 0,7242284964, Recall 0,5949298813.
- Нативные LightGBM Booster-файлы: максимальная абсолютная разница site-score,
  component-score и zone-score против исходных joblib-моделей равна 0,0;
  ранжирование top-зон совпало.
- Новый режим `safety_margin_70_50`: Precision 0,7327021122, Recall
  0,5426105717 на report-only 2026H1. Обе нижние календарные bootstrap-границы
  95% выше требований 0,70/0,50.
- API в точном общем runtime: `/health` — 200, три модели `ready`;
  `/api/v1/models/unauthorized_access/predict` — 200, JSON сериализуется,
  top-зоны возвращаются, политика ответа `P4`, автоподтверждение отключено.
- Защита актуальности: историческая дата отклоняется при
  `ML_ALLOW_HISTORICAL_SCORING=0`.
- Frontend production build: passed после разрешения локального prerender-порта.

Полный frontend lint по исходному сайту не чист: он сообщает ранее
существовавшие accessibility/React-compiler замечания в общих UI-компонентах и
`moscollector-app.tsx`. Изменение интеграции ограничено текстом счётчика моделей
и новых lint-ошибок в добавленном Python-коде не содержит.
